#!/usr/bin/env bash
# designlang Claude plugin — DISABLED: Manavarya09/design-extract repo was deleted.
# CLI (npm install -g designlang) and MCP (designlang-mcp) still work from npm.
# Plugin entry in settings.json is set to false to prevent install errors.
echo "  · designlang plugin skipped (source repo deleted — CLI + MCP still active)"
